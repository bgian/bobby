import React from 'react';
import PortfolioItemComponent from './Component';

const PortfolioItemContainer = (props) => (
  <PortfolioItemComponent {...props} />
);

export default PortfolioItemContainer;
